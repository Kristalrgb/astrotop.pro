# 📢 Инструкция по рекламным блокам

## ✅ Что добавлено:

### 1. Компонент AdBanner (`src/components/AdBanner.jsx`)
Универсальный компонент для отображения рекламы с поддержкой разных размеров и форматов.

### 2. Рекламные блоки размещены в:
- ✅ **Footer** - реклама перед копирайтом
- ✅ **Home** - реклама вверху страницы и после новостей
- ✅ **Specialists** - реклама вверху и между специалистами (после каждого 3-го)

---

## 📐 Размеры рекламы

Компонент поддерживает следующие форматы:

| Размер | Размеры | Описание |
|--------|---------|----------|
| `banner` | 728×90px | Leaderboard (стандартный горизонтальный баннер) |
| `square` | 250×250px | Квадратный блок |
| `rectangle` | 300×250px | Medium Rectangle (самый популярный формат) |
| `skyscraper` | 160×600px | Wide Skyscraper (для сайдбара) |
| `mobile` | 320×100px | Мобильный баннер |
| `large` | 970×250px | Billboard (большой горизонтальный) |
| `halfpage` | 300×600px | Half Page (для сайдбара) |

---

## 🎯 Позиции

- `top` - Вверху контейнера
- `bottom` - Внизу контейнера  
- `sidebar` - Для боковой панели
- `inline` - Встроенная в контент (по центру)

---

## 🔧 Как использовать

### Базовое использование:
```jsx
import AdBanner from '../components/AdBanner'

<AdBanner 
  size="banner" 
  position="inline" 
  id="unique-ad-id" 
  showLabel={true}
/>
```

### Примеры:

**Большой баннер вверху страницы:**
```jsx
<AdBanner 
  size="banner" 
  position="top" 
  id="top-ad" 
  showLabel={true}
/>
```

**Квадратная реклама в сайдбаре:**
```jsx
<AdBanner 
  size="square" 
  position="sidebar" 
  id="sidebar-ad" 
  showLabel={true}
/>
```

**Мобильный баннер:**
```jsx
<AdBanner 
  size="mobile" 
  position="inline" 
  id="mobile-ad" 
  showLabel={true}
/>
```

---

## 📱 Адаптивность

Рекламные блоки автоматически адаптируются под размер экрана:
- На мобильных устройствах показывается мобильный формат
- На планшетах - средний размер
- На десктопе - полный размер

---

## 🔌 Интеграция с рекламными сетями

### Google AdSense

В компоненте `AdBanner.jsx` есть закомментированный пример интеграции. Раскомментируйте и добавьте свой код:

```jsx
<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
<ins className="adsbygoogle"
     style={{display:'block'}}
     data-ad-client="ca-pub-XXXXXXXXXX"
     data-ad-slot="XXXXXXXXXX"
     data-ad-format="auto"
     data-full-width-responsive="true"></ins>
<script>
  (adsbygoogle = window.adsbygoogle || []).push({});
</script>
```

### Yandex Direct

```jsx
<div id="yandex_rtb_R-XXXXXXXX-1"></div>
<script>
  window.yaContextCb.push(() => {
    Ya.Context.AdvManager.render({
      blockId: "R-XXXXXXXX-1",
      renderTo: "yandex_rtb_R-XXXXXXXX-1"
    })
  })
</script>
```

### Собственная реклама

Замените содержимое `renderAdContent()` на ваш HTML/JSX код.

---

## 📍 Текущие места размещения

### Главная страница (Home):
1. **Вверху страницы** - баннер 728×90
2. **После новостей** - баннер 728×90

### Страница специалистов (Specialists):
1. **Вверху страницы** - баннер 728×90
2. **Между специалистами** - баннер 728×90 (после каждого 3-го)

### Footer:
1. **Перед копирайтом** - баннер 728×90

---

## 🎨 Кастомизация

### Изменить стиль рекламного блока:

Откройте `src/components/AdBanner.jsx` и найдите функцию `renderAdContent()`. Там можно изменить:
- Цвет фона
- Цвет рамки
- Размер шрифта
- Иконки

### Добавить новые места:

Просто импортируйте компонент и добавьте его:

```jsx
import AdBanner from '../components/AdBanner'

// В нужном месте:
<AdBanner size="rectangle" position="inline" id="new-ad" />
```

---

## ⚙️ Настройки

### Скрыть метку "Реклама":
```jsx
<AdBanner showLabel={false} />
```

### Изменить ID блока:
```jsx
<AdBanner id="my-custom-ad-id" />
```

---

## 📊 Аналитика

Для отслеживания кликов по рекламе можно добавить Google Analytics или другую аналитику в компонент.

---

## ✅ Готово!

Рекламные блоки готовы к использованию. Сейчас они показывают placeholder, но вы можете легко заменить их на реальную рекламу от любой рекламной сети.


