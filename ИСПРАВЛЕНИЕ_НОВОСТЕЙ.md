# 🔧 Исправление проблемы с публикацией новостей

## ❌ Проблема

При попытке опубликовать новость появляется ошибка:
- "Не удалось создать публикацию"
- "Unexpected token '<', "<!DOCTYPE "... is not valid JSON"

## 🔍 Причина

На Vercel деплоится только **фронтенд**, а **бэкенд (server/)** не запущен. Когда фронтенд пытается обратиться к `/api/news`, Vercel возвращает HTML страницу (index.html) вместо JSON ответа от сервера.

## ✅ Решение

### Вариант 1: Запустить бэкенд отдельно (РЕКОМЕНДУЕТСЯ)

#### На Railway.app (бесплатно):

1. Зайдите на https://railway.app
2. Создайте аккаунт через GitHub
3. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
4. Выберите репозиторий `astrotop.pro`
5. В настройках проекта:
   - **Root Directory:** `server`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
6. Railway даст вам URL (например: `https://ваш-проект.up.railway.app`)
7. В настройках Vercel добавьте переменную окружения:
   - `VITE_API_URL=https://ваш-проект.up.railway.app`
   - `VITE_SOCKET_URL=https://ваш-проект.up.railway.app`

#### На Render.com (бесплатно):

1. Зайдите на https://render.com
2. Создайте аккаунт
3. Нажмите **"New"** → **"Web Service"**
4. Подключите GitHub репозиторий
5. Настройки:
   - **Name:** `astrotop-backend`
   - **Root Directory:** `server`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
6. Render даст вам URL
7. Добавьте переменные окружения в Vercel (как выше)

---

### Вариант 2: Локальное хранение (временное решение)

Я уже добавил fallback на localStorage. Новости будут сохраняться локально в браузере, но они не будут видны другим пользователям.

**Ограничения:**
- Новости видны только на вашем компьютере
- При очистке кэша браузера новости пропадут
- Не синхронизируются между устройствами

---

## 🚀 Что было исправлено

1. ✅ Улучшена обработка ошибок в `NewsContext.jsx`
2. ✅ Добавлена проверка типа ответа (JSON vs HTML)
3. ✅ Добавлен fallback на localStorage
4. ✅ Улучшены сообщения об ошибках для пользователя

---

## 📝 Проверка

После настройки бэкенда:

1. Убедитесь, что переменные окружения установлены в Vercel
2. Пересоберите проект: `npm run build`
3. Задеплойте на Vercel: `git push`
4. Проверьте, что новости публикуются без ошибок

---

## 🔗 Полезные ссылки

- **Railway:** https://railway.app
- **Render:** https://render.com
- **Vercel Environment Variables:** https://vercel.com/docs/concepts/projects/environment-variables

---

## ⚠️ Важно

Для полноценной работы сайта **необходимо** задеплоить бэкенд отдельно, так как Vercel поддерживает только статические сайты и serverless функции, но не постоянные Node.js серверы.




