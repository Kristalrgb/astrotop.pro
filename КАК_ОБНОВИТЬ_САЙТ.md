# 🔄 Как обновить сайт на GitHub из Cursor

## 🎯 Краткая суть

Когда вы вносите изменения в Cursor, чтобы они появились на сайте в интернете:

1. **Сохраните изменения** в Cursor (Ctrl+S)
2. **Добавьте изменения в Git**
3. **Отправьте на GitHub**
4. **Vercel автоматически обновит сайт** (если подключен)

---

## 🚀 Быстрый способ (3 команды)

Откройте терминал в Cursor и выполните:

```powershell
git add .
git commit -m "Добавлены рекламные блоки"
git push
```

**Готово!** Сайт обновится через 1-2 минуты (если подключен к Vercel).

---

## 📝 Первый раз? Пошаговая инструкция

### Шаг 1: Проверьте, подключен ли Git

```powershell
git status
```

**Если видите ошибку** `fatal: not a git repository`:

```powershell
git init
```

---

### Шаг 2: Найдите URL вашего GitHub репозитория

1. Откройте https://github.com
2. Найдите ваш репозиторий (например, `астротоп24`)
3. Нажмите кнопку **"Code"** (зеленая)
4. Скопируйте URL (например: `https://github.com/ваш-username/астротоп24.git`)

---

### Шаг 3: Подключите локальный проект к GitHub

```powershell
git remote add origin https://github.com/ваш-username/астротоп24.git
```

**Замените URL на ваш!**

Если получили ошибку `remote origin already exists`:
```powershell
git remote remove origin
git remote add origin https://github.com/ваш-username/астротоп24.git
```

---

### Шаг 4: Добавьте все файлы

```powershell
git add .
```

---

### Шаг 5: Создайте коммит

```powershell
git commit -m "Добавлены рекламные блоки"
```

Можете написать любое описание:
- `"Добавлены рекламные блоки"`
- `"Обновлен дизайн"`
- `"Исправлены ошибки"`

---

### Шаг 6: Отправьте на GitHub

**Первый раз:**
```powershell
git push -u origin main
```

**В следующий раз:**
```powershell
git push
```

---

### Шаг 7: Авторизация (если попросит)

GitHub может попросить ввести:
- **Username:** ваш GitHub username
- **Password:** Personal Access Token (НЕ пароль!)

#### Как получить токен:

1. Зайдите на https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Дайте имя (например: "Мой компьютер")
4. Выберите срок (например: "90 days")
5. Поставьте галочку **"repo"**
6. Нажмите **"Generate token"**
7. **Скопируйте токен** (показывается один раз!)

Используйте этот токен как пароль.

---

## 🔄 Регулярное обновление (после каждого изменения)

Когда вы изменили что-то в Cursor:

```powershell
# 1. Посмотреть, что изменилось
git status

# 2. Добавить изменения
git add .

# 3. Создать коммит
git commit -m "Описание ваших изменений"

# 4. Отправить на GitHub
git push
```

**Или всё сразу:**
```powershell
git add . && git commit -m "Обновление" && git push
```

---

## 🌐 Автоматическое обновление сайта

Если ваш сайт подключен к GitHub через Vercel:

✅ **Изменения применятся автоматически!**

После `git push`:
1. Vercel обнаружит изменения
2. Запустит сборку
3. Обновит сайт через 1-2 минуты

**Проверить:** https://vercel.com/dashboard

---

## 🤖 Автоматический скрипт

Если не хотите вводить команды вручную:

```powershell
.\update-github.ps1
```

Скрипт спросит:
- Добавить все файлы? (y/n)
- Сообщение коммита?
- Подключен ли репозиторий?

И выполнит всё автоматически!

---

## ❓ Частые проблемы

### "fatal: not a git repository"
```powershell
git init
```

### "remote origin already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/ваш-username/астротоп24.git
```

### "failed to push some refs"
```powershell
git pull origin main --allow-unrelated-histories
git push
```

### "Authentication failed"
- Используйте Personal Access Token, а не пароль
- См. Шаг 7 выше

### "branch 'main' does not exist"
```powershell
git branch -M main
git push -u origin main
```

---

## 📊 Проверка текущего состояния

```powershell
# Статус изменений
git status

# Подключенные репозитории
git remote -v

# История коммитов
git log --oneline
```

---

## ✅ Чек-лист перед отправкой

- [ ] Все файлы сохранены (Ctrl+S)
- [ ] Изменения протестированы локально
- [ ] Создан понятный коммит
- [ ] Выполнен `git add .`
- [ ] Выполнен `git commit`
- [ ] Выполнен `git push`

---

## 🎯 Примеры использования

**Добавили рекламу:**
```powershell
git add .
git commit -m "Добавлены рекламные блоки на главную и страницу специалистов"
git push
```

**Исправили баг:**
```powershell
git add .
git commit -m "Исправлена ошибка в расчете стоимости консультации"
git push
```

**Обновили дизайн:**
```powershell
git add .
git commit -m "Обновлен дизайн кнопок и улучшена адаптивность"
git push
```

---

## 📚 Дополнительные материалы

- **Подробная инструкция:** `ОБНОВИТЬ_НА_GITHUB.md`
- **Быстрая инструкция:** `БЫСТРОЕ_ОБНОВЛЕНИЕ_GITHUB.txt`
- **Автоматический скрипт:** `update-github.ps1`

---

## 🎉 Готово!

Теперь вы знаете, как обновлять сайт на GitHub из Cursor. 

**Помните:** 
- Сохраните изменения → `git add .` → `git commit -m "..."` → `git push`
- Vercel обновит сайт автоматически!

**Удачи!** 🚀




