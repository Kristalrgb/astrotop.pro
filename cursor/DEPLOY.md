# 🚀 Инструкция по деплою сайта "Астротоп24"

## 📋 Обзор

Проект состоит из двух частей:
1. **Frontend** (React + Vite) - деплоится на Vercel
2. **Backend** (Node.js + Express + Socket.IO) - деплоится на Railway/Render

---

## 🔧 Шаг 1: Деплой Frontend на Vercel

### 1.1. Установка Vercel CLI (если еще не установлен)

```bash
npm install -g vercel
```

### 1.2. Вход в аккаунт Vercel

```bash
vercel login
```

Откроется браузер для входа через GitHub/Email.

### 1.3. Проверка сборки проекта

Убедитесь, что проект собирается без ошибок:

```bash
npm run build
```

### 1.4. Деплой на Vercel

#### Первый деплой (preview):
```bash
vercel
```

#### Продакшен деплой:
```bash
vercel --prod
```

**Ответьте на вопросы:**
- `Link to existing project?` → `N` (если первый раз)
- `Project name?` → нажмите Enter (использует имя папки)
- `Directory?` → нажмите Enter (текущая папка)
- `Override settings?` → `N`

### 1.5. Получение URL

После успешного деплоя вы получите:
- Preview URL: `https://ваш-проект-git-xxx.vercel.app`
- Production URL: `https://ваш-проект.vercel.app`

---

## 🖥️ Шаг 2: Деплой Backend на Railway (рекомендуется)

### 2.1. Регистрация на Railway

1. Перейдите на https://railway.app
2. Зарегистрируйтесь через GitHub

### 2.2. Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Подключите ваш репозиторий
4. Выберите папку `server` как корневую

### 2.3. Настройка проекта

В настройках проекта Railway:
- **Root Directory:** `server`
- **Build Command:** `npm install`
- **Start Command:** `npm start`

### 2.4. Настройка переменных окружения

В Railway добавьте переменные (если нужны):
- `PORT` (Railway установит автоматически)
- `NODE_ENV=production`

### 2.5. Получение URL

Railway автоматически предоставит URL типа:
`https://ваш-проект.up.railway.app`

**Сохраните этот URL!** Он понадобится для настройки фронтенда.

---

## 🖥️ Альтернатива: Деплой Backend на Render.com

### 1. Регистрация на Render

1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub

### 2. Создание Web Service

1. Нажмите "New +" → "Web Service"
2. Подключите ваш GitHub репозиторий

### 3. Настройки

- **Name:** `astrotop24-backend`
- **Root Directory:** `server`
- **Environment:** `Node`
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Plan:** Free

### 4. Получение URL

Render предоставит URL типа:
`https://ваш-проект.onrender.com`

---

## ⚙️ Шаг 3: Настройка переменных окружения

### 3.1. В Vercel (Frontend)

1. Зайдите на https://vercel.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings → Environment Variables**
4. Добавьте переменные:

```
VITE_SOCKET_URL=https://ваш-backend-url.com
VITE_API_URL=https://ваш-backend-url.com
```

Замените `https://ваш-backend-url.com` на реальный URL вашего бэкенда (Railway или Render).

5. Нажмите "Save"
6. **ВАЖНО:** Передеплойте проект, чтобы переменные вступили в силу:
   - Зайдите в **Deployments**
   - Нажмите на последний деплой
   - Нажмите "Redeploy"

### 3.2. В Railway/Render (Backend)

Убедитесь, что CORS настроен правильно. В `server/index.js` уже есть настройка CORS.

Если нужно добавить переменные:
- `PORT` (обычно устанавливается автоматически)
- `NODE_ENV=production`

---

## 🔗 Шаг 4: Настройка домена (опционально)

### 4.1. Подключение кастомного домена в Vercel

1. Зайдите в **Settings → Domains** вашего проекта на Vercel
2. Добавьте домен: `astrotop.pro`
3. Также добавьте: `www.astrotop.pro`
4. Vercel покажет DNS записи для добавления

### 4.2. Настройка DNS записей

В панели управления вашего домена добавьте записи:

**Для apex домена (astrotop.pro):**
- Тип: `A`
- Имя: `@`
- Значение: IP адрес от Vercel (будет показан)

**Для www поддомена:**
- Тип: `CNAME`
- Имя: `www`
- Значение: `cname.vercel-dns.com`

### 4.3. Подключение домена к Backend (Railway)

1. В Railway перейдите в настройки проекта
2. Откройте вкладку **Domains**
3. Добавьте кастомный домен (если есть)
4. Или используйте домен от Railway

---

## ✅ Шаг 5: Проверка работы

### 5.1. Проверка Frontend

1. Откройте ваш сайт на Vercel
2. Проверьте, что все страницы открываются
3. Проверьте консоль браузера на ошибки

### 5.2. Проверка Backend

1. Откройте: `https://ваш-backend-url.com/api/languages`
2. Должен вернуться JSON со списком языков

### 5.3. Проверка Socket.IO

1. Откройте консоль браузера на сайте
2. Проверьте, что нет ошибок подключения к Socket.IO
3. Попробуйте создать видеоконсультацию

---

## 🔄 Автоматический деплой через GitHub

### Настройка авто-деплоя:

1. **Vercel:**
   - При подключении репозитория Vercel автоматически настроит деплой
   - Каждый push в `main` ветку будет автоматически деплоиться

2. **Railway:**
   - Railway также автоматически деплоит при каждом push
   - Проверьте настройки в панели Railway

---

## 🐛 Решение проблем

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Проверьте переменные окружения в Vercel
2. Убедитесь, что URL бэкенда правильный
3. Проверьте CORS настройки на бэкенде
4. Передеплойте фронтенд после изменения переменных

### Проблема: Socket.IO не работает

**Решение:**
1. Убедитесь, что бэкенд запущен и доступен
2. Проверьте переменную `VITE_SOCKET_URL`
3. Проверьте, что WebSocket поддерживается (не все бесплатные платформы поддерживают)

### Проблема: Ошибки сборки на Vercel

**Решение:**
1. Проверьте логи сборки в Vercel Dashboard
2. Убедитесь, что `npm run build` работает локально
3. Проверьте версии Node.js (Vercel использует 18.x)

---

## 📝 Чек-лист деплоя

- [ ] Frontend собран успешно локально
- [ ] Frontend задеплоен на Vercel
- [ ] Backend задеплоен на Railway/Render
- [ ] Переменные окружения настроены в Vercel
- [ ] URL бэкенда добавлен в переменные фронтенда
- [ ] Проект передеплоен после настройки переменных
- [ ] Сайт открывается и работает
- [ ] API запросы работают
- [ ] Socket.IO подключается
- [ ] Домен настроен (если нужен)

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен в интернете!

**Frontend:** `https://ваш-проект.vercel.app`  
**Backend:** `https://ваш-проект.up.railway.app`

---

## 📞 Полезные ссылки

- **Vercel Dashboard:** https://vercel.com/dashboard
- **Railway Dashboard:** https://railway.app/dashboard
- **Render Dashboard:** https://dashboard.render.com
- **Vercel CLI Docs:** https://vercel.com/docs/cli



