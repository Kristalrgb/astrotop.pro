# 🚀 Как обновить сайт на GitHub (из Cursor)

## 📋 Быстрая инструкция

### Вариант 1: Репозиторий уже существует на GitHub

Если у вас уже есть репозиторий на GitHub:

1. **Откройте ваш репозиторий на GitHub** и скопируйте URL (например: `https://github.com/ваш-username/астротоп24.git`)

2. **В терминале Cursor выполните:**

```powershell
# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Добавлены рекламные блоки"

# Подключение к GitHub репозиторию (замените URL на ваш)
git remote add origin https://github.com/ваш-username/астротоп24.git

# Отправка на GitHub
git push -u origin main
```

**Если ветка называется `master` вместо `main`:**
```powershell
git push -u origin master
```

---

### Вариант 2: Репозиторий уже подключен

Если репозиторий уже подключен, просто выполните:

```powershell
# Проверить статус
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Добавлены рекламные блоки и обновления"

# Отправить на GitHub
git push
```

---

## 🔄 Регулярное обновление (после каждого изменения)

Когда вы вносите изменения в Cursor и хотите обновить GitHub:

```powershell
git add .
git commit -m "Описание изменений"
git push
```

---

## 📝 Примеры сообщений коммитов

- `"Добавлены рекламные блоки"`
- `"Исправлена адаптивность для мобильных"`
- `"Обновлен дизайн кнопок"`
- `"Добавлена функция перевода"`

---

## ⚠️ Если возникла ошибка

### Ошибка: "remote origin already exists"
```powershell
# Удалить старый remote
git remote remove origin

# Добавить заново
git remote add origin https://github.com/ваш-username/астротоп24.git
```

### Ошибка: "failed to push some refs"
```powershell
# Сначала получить изменения с GitHub
git pull origin main --allow-unrelated-histories

# Затем отправить свои
git push
```

---

## 🌐 Автоматическое обновление сайта на Vercel

Если ваш сайт подключен к GitHub через Vercel:

✅ **Изменения автоматически применятся!**

После `git push`:
1. Vercel обнаружит изменения
2. Запустит автоматическую сборку
3. Обновит сайт через 1-2 минуты

Проверить можно в панели Vercel: https://vercel.com/dashboard

---

## 🔍 Проверка текущего состояния

```powershell
# Посмотреть статус
git status

# Посмотреть подключенные репозитории
git remote -v

# Посмотреть историю коммитов
git log --oneline
```

---

## ✅ Чек-лист перед отправкой

- [ ] Все файлы сохранены в Cursor
- [ ] Изменения протестированы локально
- [ ] Создан понятный коммит с описанием
- [ ] Выполнен `git add .`
- [ ] Выполнен `git commit`
- [ ] Выполнен `git push`

---

## 📞 Нужна помощь?

Если что-то не работает, проверьте:
1. Установлен ли Git: `git --version`
2. Настроен ли GitHub аккаунт: `git config --global user.name` и `git config --global user.email`
3. Есть ли доступ к репозиторию на GitHub




