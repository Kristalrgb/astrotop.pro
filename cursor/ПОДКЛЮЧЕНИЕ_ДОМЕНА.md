# 🔗 Подключение домена astrotop.pro к сайту

## ⚡ Быстрая шпаргалка для imena.ua

**Что нужно добавить в DNS настройках imena.ua:**

```
Тип записи: A
Имя/Host: @ (или оставьте ПУСТЫМ)
Значение/IP: 216.198.79.1
TTL: (если есть поле - оставьте по умолчанию, если нет - пропустите)
```

**⚠️ Важно:** Поле TTL может отсутствовать - это нормально! Панель установит его автоматически.

**Где найти:** imena.ua → Ваш домен → "DNS записи" или "Управление DNS"

**После добавления:** Подождите 5-30 минут, затем нажмите "Refresh" в Vercel

---

## 📋 Что нужно из панели управления доменом

Для подключения вашего домена `astrotop.pro` к деплою на Vercel/Netlify вам понадобится доступ к **DNS настройкам**.

## 🎯 Вариант 1: Vercel (Рекомендуется)

### Шаг 1: Деплой проекта на Vercel

Сначала задеплойте проект (получите временный URL):

```bash
npm install -g vercel
vercel login
vercel --prod
```

Вы получите URL типа: `https://ваш-проект.vercel.app`

### Шаг 2: Добавление домена в Vercel

1. Зайдите в настройки проекта на Vercel
2. Перейдите в **Settings → Domains**
3. Добавьте домен: `astrotop.pro`
4. Также добавьте: `www.astrotop.pro` (опционально)
5. Vercel покажет вам **DNS записи**, которые нужно добавить

### Шаг 3: Настройка DNS в панели домена imena.ua

#### Пошаговая инструкция для imena.ua:

1. **Войдите в панель управления imena.ua:**
   - Откройте https://imena.ua
   - Войдите в свой аккаунт

2. **Найдите ваш домен:**
   - В списке доменов найдите `astrotop.pro`
   - Нажмите на него или на кнопку "Управление"

3. **Откройте DNS настройки:**
   - Найдите раздел **"DNS записи"** или **"Налаштування DNS"**
   - Или перейдите в **"Управление доменом" → "DNS записи"**

4. **⚠️ ВАЖНО: Сначала удалите старые конфликтующие записи:**
   
   **Найдите и УДАЛИТЕ** следующие записи (если они есть в списке):
   - ❌ **Тип:** `A`, **Имя:** `@`, **Значение:** `5.39.10.93` - **УДАЛИТЬ**
   - ❌ **Тип:** `AAAA`, **Имя:** `@`, **Значение:** `2001:41d0:203:dee4:5:39:10:93` - **УДАЛИТЬ**
   
   **Как удалить:** Найдите запись в списке и нажмите кнопку "Удалить" / "Delete" рядом с ней

5. **Добавьте новую A запись для корневого домена:**
   
   Нажмите **"Добавить запись"** или **"+"** и заполните:
   
   - **Тип записи:** выберите `A` (или `A Record`)
   - **Имя (Host):** введите `@` или оставьте **пустым** (в некоторых панелях это может быть поле "Поддомен" - оставьте пустым)
   - **Значение (Value/IP):** введите `216.198.79.1` ⬅️ **Это новый IP от Vercel!**
   - **TTL:** если есть поле - оставьте по умолчанию, если нет - пропустите (устанавливается автоматически)
   
   Нажмите **"Сохранить"** или **"Добавить"**
   
   **Примечание:** imena.ua может показать сообщение о том, что изменения вступят в силу через 24 часа - это нормально! Обычно изменения применяются быстрее (5-30 минут или 2-4 часа).

6. **Опционально: Добавьте CNAME для www (если нужно):**
   
   Если хотите, чтобы `www.astrotop.pro` тоже работал:
   - **Тип записи:** `CNAME`
   - **Имя:** `www`
   - **Значение:** `cname.vercel-dns.com`
   - **TTL:** `3600`

**Важно:** 
- В панели imena.ua поле "Имя" для корневого домена может называться "Host" или "Поддомен"
- Для корневого домена (`astrotop.pro`) используйте `@` или оставьте поле **пустым**
- Точный IP адрес (`216.198.79.1`) скопируйте из Vercel (Settings → Domains → DNS Records)

---

## 🎯 Вариант 2: Netlify

### Шаг 1: Деплой на Netlify

```bash
npm run build
# Перетащите папку dist на Netlify или используйте CLI
```

### Шаг 2: Добавление домена в Netlify

1. Зайдите в **Site settings → Domain management**
2. Добавьте домен: `astrotop.pro`
3. Netlify покажет DNS записи для добавления

### Шаг 3: Настройка DNS в панели домена

Зайдите в **"Налаштування домену (DNS та редиректи)"** и добавьте записи:

**Тип:** `A`
**Имя:** `@`
**Значение:** IP адрес от Netlify (обычно что-то вроде `75.2.60.5`)

**Тип:** `CNAME`
**Имя:** `www`
**Значение:** `ваш-проект.netlify.app`

---

## 📝 Пример заполнения DNS записей

### Для Vercel (актуальные данные на 2024):

```
Тип: A
Имя: @ (или пустое поле)
Значение: 216.198.79.1
TTL: (если есть - оставьте по умолчанию, если нет - пропустите)

Тип: CNAME
Имя: www
Значение: cname.vercel-dns.com
TTL: (если есть - оставьте по умолчанию, если нет - пропустите)
```

**⚠️ ВАЖНО:** Vercel обновил IP адреса. Используйте `216.198.79.1` вместо старых значений!

### Для Netlify:

```
Тип: A
Имя: @
Значение: 75.2.60.5
TTL: 3600

Тип: CNAME
Имя: www
Значение: ваш-проект.netlify.app
TTL: 3600
```

---

## ⏱️ Время активации DNS

После добавления DNS записей:
- ⏰ Обычно занимает **5-30 минут**
- 🔄 Может занять до **24-48 часов** (максимум)
- ✅ Проверьте статус на Vercel/Netlify - там покажут, когда домен подключен

---

## 🔍 Как проверить, что DNS настроены правильно

### Через командную строку:
```bash
# Проверка A записи
nslookup astrotop.pro

# Проверка CNAME записи
nslookup www.astrotop.pro
```

### Через онлайн сервисы:
- https://dnschecker.org
- https://www.whatsmydns.net

---

## ⚙️ Альтернатива: Изменение NS серверов

Вместо настройки отдельных DNS записей можно изменить **NS сервери** (Name Servers):

1. В панели домена зайдите в **"NS сервери"**
2. Измените NS серверы на те, что даст Vercel/Netlify
3. Это передаст полное управление DNS вашему хостингу

**Для Vercel NS серверы:**
```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

**Для Netlify NS серверы:**
```
dns1.p01.nsone.net
dns2.p01.nsone.net
dns3.p01.nsone.net
dns4.p01.nsone.net
```

---

## 🎯 Специальная инструкция для imena.ua

### Пошаговое руководство:

1. **Войдите в imena.ua:**
   - Откройте https://imena.ua и войдите в аккаунт

2. **Откройте управление доменом:**
   - В списке доменов найдите `astrotop.pro`
   - Нажмите на домен или кнопку "Управление"

3. **Найдите раздел DNS:**
   - Ищите: **"DNS записи"**, **"Управление DNS"**, **"Налаштування DNS"** или **"DNS настройки"**
   - В некоторых версиях панели это может быть в разделе **"Дополнительно"** или **"Технические настройки"**

4. **Добавьте A запись:**
   
   Нажмите **"Добавить запись"**, **"Создать запись"** или кнопку **"+"**
   
   Заполните форму:
   ```
   Тип записи: A (или A Record)
   Имя/Host/Поддомен: @ (или оставьте ПУСТЫМ)
   Значение/IP адрес: 216.198.79.1
   TTL: (если есть поле - оставьте по умолчанию или пропустите)
   ```
   
   **Варианты названий полей в imena.ua:**
   - "Имя" / "Host" / "Поддомен" → для корневого домена используйте `@` или оставьте **пустым**
   - "Значение" / "IP адрес" / "Адрес" → введите `216.198.79.1`
   - "TTL" → **если поля нет - это нормально!** Многие панели устанавливают TTL автоматически. Если поле есть - оставьте по умолчанию или пропустите

5. **Сохраните запись:**
   - Нажмите **"Сохранить"**, **"Добавить"** или **"Создать"**

6. **Проверьте результат:**
   - В списке DNS записей должна появиться запись типа `A` с IP `216.198.79.1`
   - Если есть старые A записи для `@` - можно их оставить или удалить

7. **Вернитесь в Vercel:**
   - Откройте Vercel → Settings → Domains
   - Нажмите кнопку **"Refresh"** рядом с доменом `astrotop.pro`
   - Подождите 5-30 минут - статус должен измениться на "Valid Configuration" ✅

### 🔍 Если не можете найти DNS настройки в imena.ua:

1. Попробуйте поиск по странице (Ctrl+F) слова: "DNS", "записи", "A record"
2. Проверьте разделы: "Управление доменом", "Технические настройки", "Дополнительно"
3. Если используете NS серверы другого провайдера - нужно менять DNS там
4. Обратитесь в поддержку imena.ua - они подскажут, где находятся DNS записи

---

## ✅ Быстрая инструкция (Vercel)

1. ✅ Задеплойте проект: `vercel --prod`
2. ✅ В настройках проекта добавьте домен `astrotop.pro`
3. ✅ Скопируйте DNS записи из Vercel (IP: `216.198.79.1`)
4. ✅ В панели imena.ua зайдите в **"DNS записи"** или **"Управление DNS"**
5. ✅ Добавьте A запись: тип `A`, имя `@` (или пустое), значение `216.198.79.1`
6. ✅ Нажмите "Refresh" в Vercel и подождите 5-30 минут
7. ✅ Готово! Ваш сайт будет доступен на `https://astrotop.pro`

---

## 🆘 Если что-то не работает

### Проблемы с imena.ua:

1. **Не могу найти раздел DNS записей:**
   - Попробуйте разные разделы: "Управление доменом", "Технические настройки", "Дополнительно"
   - Используйте поиск по странице (Ctrl+F): "DNS", "A record", "записи"
   - Обратитесь в поддержку imena.ua - они точно подскажут

2. **Поле "Имя" не принимает @:**
   - Попробуйте оставить поле **полностью пустым** (это означает корневой домен)
   - Или введите просто точку `.`
   - В некоторых панелях для корневого домена вообще не нужно указывать имя

3. **Запись не сохраняется:**
   - Проверьте, что IP адрес введен правильно: `216.198.79.1` (без пробелов)
   - Убедитесь, что тип записи выбран `A` (не AAAA, не CNAME)
   - Проверьте, нет ли уже существующей A записи для `@` - возможно нужно ее отредактировать

4. **Vercel все еще показывает "Invalid Configuration":**
   - Подождите 10-30 минут после добавления записи
   - Нажмите кнопку **"Refresh"** в Vercel (Settings → Domains)
   - Проверьте DNS через https://dnschecker.org - введите `astrotop.pro` и выберите тип `A`
   - Убедитесь, что DNS записи распространились (может занять до 48 часов)

### Общие проблемы:

1. **Проверьте DNS записи** через dnschecker.org
2. **Убедитесь, что записи добавлены правильно** (без лишних пробелов, правильный IP)
3. **Подождите больше времени** (иногда нужно до 48 часов)
4. **Проверьте SSL сертификат** - Vercel обычно выдает его автоматически после подключения домена
5. **Очистите кэш DNS** на вашем компьютере: 
   - Windows: `ipconfig /flushdns` в командной строке
   - Mac/Linux: `sudo dscacheutil -flushcache` или `sudo systemd-resolve --flush-caches`

---

## 📞 Что именно нужно из панели

Вам понадобится доступ к:
- ✅ **"Налаштування домену (DNS та редиректи)"** - для добавления A и CNAME записей
- ✅ Или **"NS сервери"** - если хотите передать управление DNS хостингу

**Все остальное в панели (контакты, автопродление и т.д.) для деплоя не нужно!**






