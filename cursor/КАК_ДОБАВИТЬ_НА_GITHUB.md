# 📤 Как добавить изменения на GitHub

## 📋 Пошаговая инструкция

### Шаг 1: Проверьте, установлен ли Git

Откройте PowerShell и выполните:
```bash
git --version
```

Если Git не установлен:
- Скачайте с https://git-scm.com/download/win
- Установите и перезапустите PowerShell

---

### Шаг 2: Инициализируйте Git репозиторий

```bash
git init
```

---

### Шаг 3: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите кнопку **"+"** (справа вверху)
3. Выберите **"New repository"**
4. Введите имя: `астротоп24` (или любое другое)
5. Выберите **Public** (или Private, если хотите приватный)
6. **НЕ** ставьте галочки "Add README", "Add .gitignore", "Add license"
7. Нажмите **"Create repository"**

---

### Шаг 4: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните в PowerShell:

```bash
git remote add origin https://github.com/ВАШ-USERNAME/астротоп24.git
```

**Замените `ВАШ-USERNAME` на ваш GitHub username!**

Например:
```bash
git remote add origin https://github.com/misha-laptop/астротоп24.git
```

---

### Шаг 5: Добавьте все файлы в Git

```bash
git add .
```

Эта команда добавит все файлы проекта (кроме тех, что в .gitignore).

---

### Шаг 6: Сделайте первый коммит

```bash
git commit -m "Первая версия сайта с функциями: первые 3 минуты бесплатно и адаптивность"
```

---

### Шаг 7: Переименуйте ветку в main (если нужно)

```bash
git branch -M main
```

---

### Шаг 8: Отправьте на GitHub

```bash
git push -u origin main
```

Вас попросят ввести логин и пароль. Используйте:
- **Username:** ваш GitHub username
- **Password:** Personal Access Token (НЕ пароль от GitHub!)

---

## 🔑 Как получить Personal Access Token:

1. Зайдите на https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Дайте имя токену (например: "Мой компьютер")
4. Выберите срок действия (например: "90 days" или "No expiration")
5. Поставьте галочку **"repo"** (это даст доступ к репозиториям)
6. Нажмите **"Generate token"** внизу
7. **Скопируйте токен** (он показывается только один раз!)

Используйте этот токен как пароль при `git push`.

---

## 🔄 Как обновить изменения в будущем

После того, как вы внесли изменения в код:

```bash
# 1. Посмотрите, что изменилось
git status

# 2. Добавьте все изменения
git add .

# 3. Сделайте коммит с описанием изменений
git commit -m "Описание ваших изменений"

# 4. Отправьте на GitHub
git push
```

### Примеры сообщений коммита:

```bash
git commit -m "Добавлена функция первых 3 минут бесплатно"
git commit -m "Улучшена адаптивность для мобильных"
git commit -m "Исправлена ошибка в расчете стоимости"
git commit -m "Обновлен дизайн кнопок"
```

---

## 🔗 Подключение GitHub к Vercel (автоматический деплой)

После того, как код на GitHub:

1. Зайдите на https://vercel.com
2. Нажмите **"Add New Project"**
3. Выберите **"Import Git Repository"**
4. Нажмите **"Configure GitHub App"** (если нужно)
5. Выберите ваш репозиторий `астротоп24`
6. Нажмите **"Import"**
7. Vercel автоматически определит настройки (Vite/React)
8. Нажмите **"Deploy"**

Теперь при каждом `git push` на GitHub, Vercel автоматически обновит сайт! 🚀

---

## ✅ Быстрая команда для обновления (все в одной строке)

```bash
git add . && git commit -m "Обновление: новые функции" && git push
```

---

## 📝 Текущие изменения, которые нужно добавить:

✅ Функция "первые 3 минуты бесплатно"
✅ Улучшена адаптивность для мобильных и планшетов
✅ Обновлен PaymentContext
✅ Добавлен таймер в VideoChat
✅ Обновлены компоненты SpecialistCard и PaymentModal

---

## ❓ Проблемы?

### Ошибка "fatal: not a git repository"
Выполните: `git init`

### Ошибка "remote origin already exists"
Выполните:
```bash
git remote remove origin
git remote add origin https://github.com/ВАШ-USERNAME/астротоп24.git
```

### Ошибка аутентификации
Убедитесь, что используете Personal Access Token, а не пароль.

### Ошибка "failed to push some refs"
Выполните:
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

---

## 🎯 Готово!

После выполнения всех шагов:
- ✅ Код на GitHub
- ✅ Можно подключить к Vercel для автоматического деплоя
- ✅ Можно делиться ссылкой на репозиторий

**Удачи!** 🚀


