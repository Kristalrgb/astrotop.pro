# 🚀 Как запустить сайт "Астро Консультации"

## 📋 Пошаговая инструкция

### 1️⃣ **Запуск локально (на вашем компьютере):**

**Шаг 1: Откройте терминал в папке проекта**
```bash
cd C:\Users\Misha-Laptop\Desktop\астротоп24
```

**Шаг 2: Запустите сервер разработки**
```bash
npm run dev
```

**Шаг 3: Откройте в браузере**
- Откройте браузер (Chrome, Firefox, Edge)
- Введите адрес: **http://localhost:3000/**
- Или нажмите Ctrl+клик на ссылку в терминале

**✅ Готово!** Сайт должен открыться в браузере.

---

### 2️⃣ **Запуск на сервере в интернете:**

#### **Вариант А: Бесплатный хостинг (Vercel, Netlify, GitHub Pages)**

**Vercel (рекомендуется):**

1. **Создайте аккаунт** на https://vercel.com
2. **Установите Vercel CLI:**
   ```bash
   npm install -g vercel
   ```

3. **Зайдите в папку проекта и войдите:**
   ```bash
   cd C:\Users\Misha-Laptop\Desktop\астротоп24
   vercel login
   ```

4. **Запустите деплой:**
   ```bash
   vercel
   ```

5. **Следуйте инструкциям:**
   - Нажмите Enter для подтверждения
   - Выберите проект (если есть)
   - Дождитесь завершения

6. **Получите ссылку:**
   - Vercel даст вам ссылку типа: `https://ваш-проект.vercel.app`

**Netlify:**

1. **Создайте аккаунт** на https://www.netlify.com
2. **Соберите проект:**
   ```bash
   npm run build
   ```

3. **Перетащите папку `dist`** на https://app.netlify.com/drop

4. **Получите ссылку:**
   - Netlify даст вам ссылку типа: `https://ваш-проект.netlify.app`

**GitHub Pages:**

1. **Создайте репозиторий на GitHub**
2. **Загрузите код:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git push -u origin main
   ```

3. **Настройте GitHub Pages в настройках репозитория**

---

#### **Вариант Б: Платный хостинг (VPS, хостинг-провайдеры)**

**Рекомендуемые хостинги:**

1. **Hostinger** - от 200₽/мес
2. **Timeweb** - от 150₽/мес (российский)
3. **Reg.ru** - от 99₽/мес (российский)
4. **DigitalOcean** - от $6/мес
5. **AWS** - плати по мере использования

**Инструкция для VPS:**

1. **Купите VPS** у провайдера
2. **Подключитесь по SSH:**
   ```bash
   ssh user@ваш-сервер-ip
   ```

3. **Установите Node.js:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

4. **Загрузите проект:**
   ```bash
   git clone ваш-репозиторий
   cd ваш-проект
   npm install
   npm run build
   ```

5. **Запустите с PM2:**
   ```bash
   npm install -g pm2
   pm2 start npm --name "astro-consultations" -- start
   pm2 save
   pm2 startup
   ```

6. **Настройте Nginx:**
   ```bash
   sudo apt-get install nginx
   sudo nano /etc/nginx/sites-available/default
   ```

   Добавьте конфигурацию:
   ```nginx
   server {
       listen 80;
       server_name ваш-домен.ru;
       
       location / {
           root /var/www/ваш-проект/dist;
           try_files $uri /index.html;
       }
   }
   ```

7. **Перезапустите Nginx:**
   ```bash
   sudo systemctl restart nginx
   ```

---

## 🛠 **Быстрая команда для запуска:**

### Локально:
```bash
cd C:\Users\Misha-Laptop\Desktop\астротоп24 && npm run dev
```

### В интернете (Vercel):
```bash
npm install -g vercel
cd C:\Users\Misha-Laptop\Desktop\астротоп24
vercel --prod
```

---

## ⚠️ **Перед деплоем убедитесь:**

1. ✅ Сайт работает локально (`npm run dev`)
2. ✅ Нет ошибок в консоли
3. ✅ Все файлы на месте
4. ✅ Проект собирается (`npm run build`)

---

## 📱 **Мобильная версия:**

Если хотите сделать мобильное приложение:
- **React Native** - для iOS и Android
- **PWA** - Progressive Web App (работает как приложение)

---

**Нужна помощь с конкретным шагом? Напишите, какой вариант вас интересует!** 🚀






