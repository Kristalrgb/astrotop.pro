# 🟢 Фильтр "Онлайн" для специалистов

## Обзор

Добавлен новый фильтр "Только онлайн" в разделе специалистов, который позволяет показывать только тех астрологов и тарологов, кто готов к консультации прямо сейчас.

## 🎯 Функциональность

### Что добавлено:
- ✅ Чекбокс "Только онлайн" в фильтрах
- ✅ Анимированный индикатор статуса (пульсирующая зеленая точка)
- ✅ Фильтрация по полю `isOnline`
- ✅ Обновленная отладочная информация
- ✅ CSS анимация для визуального эффекта

### Логика работы:
1. **Проверка статуса:** Фильтр проверяет поле `isOnline` у каждого специалиста
2. **Визуальная индикация:** Зеленая пульсирующая точка показывает активность
3. **Мгновенная фильтрация:** Результаты обновляются в реальном времени
4. **Совместимость:** Работает совместно с другими фильтрами (специализация, поиск, сортировка)

## 🛠 Технические детали

### Обновленные файлы:
- `src/pages/Specialists.jsx` - добавлен фильтр и логика
- `src/contexts/SpecialistsContext.jsx` - добавлено поле `isOnline`

### Новые состояния:
```javascript
const [onlineOnly, setOnlineOnly] = useState(false)
```

### Логика фильтрации:
```javascript
// Фильтр "Онлайн" - показываем только тех, кто готов к консультации
if (onlineOnly) {
  result = result.filter(specialist => specialist.isOnline === true)
}
```

### CSS анимация:
```css
@keyframes pulse {
  0% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.5; transform: scale(1.1); }
  100% { opacity: 1; transform: scale(1); }
}
```

## 🎨 UI/UX особенности

### Визуальные элементы:
- **Чекбокс** с красивым дизайном
- **Анимированная точка** (зеленая, пульсирующая)
- **Текст "Только онлайн"** с иконкой
- **Адаптивная верстка** для всех устройств

### Интерактивность:
- Мгновенное применение фильтра
- Сохранение состояния при переключении других фильтров
- Отладочная информация в консоли

## 📊 Статусы специалистов

### Текущие настройки:
- **Елена Петрова** (Астролог) - `isOnline: true` ✅
- **Михаил Сидоров** (Таролог) - `isOnline: false` ❌
- **Анна Козлова** (Астролог-нумеролог) - `isOnline: true` ✅
- **Дмитрий Волков** (Астролог) - `isOnline: false` ❌
- **Мария Иванова** (Таролог-медиум) - `isOnline: true` ✅

## 🚀 Использование

### Для пользователей:
1. Перейти на страницу "Специалисты"
2. Нажать кнопку "Фильтр"
3. Поставить галочку "Только онлайн"
4. Увидеть только доступных специалистов

### Для разработчиков:
```javascript
// Добавление нового специалиста с онлайн статусом
const newSpecialist = {
  // ... другие поля
  isOnline: true, // или false
  available: true
}
```

## 🔧 Настройка

### Изменение статуса специалиста:
```javascript
// В контексте специалистов
updateSpecialist({
  ...specialist,
  isOnline: true // или false
})
```

### Добавление новых статусов:
Можно расширить систему, добавив:
- `lastSeen` - время последней активности
- `status` - более детальные статусы (занят, свободен, офлайн)
- `workingHours` - рабочие часы

## 📱 Адаптивность

Фильтр полностью адаптивен и работает на:
- 🖥️ Десктоп (1200px+)
- 💻 Планшеты (768px - 1199px)
- 📱 Мобильные устройства (до 767px)

## 🎯 Результат

Теперь пользователи могут:
- ✅ Быстро найти доступных специалистов
- ✅ Не тратить время на недоступных консультантов
- ✅ Получить мгновенную консультацию
- ✅ Видеть актуальный статус в реальном времени

---

**Создано для проекта "Астро Консультации"** 🌟








